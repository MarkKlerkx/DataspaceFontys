apiVersion: v1
kind: Service
metadata:
  name: ingress-url-portal
  namespace: kube-system
  labels:
    app: ingress-url-portal
spec:
  type: NodePort
  selector:
    app: ingress-url-portal
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30091

