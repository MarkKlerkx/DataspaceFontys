# Apply after cert-manager has created secret `ca-secret` in namespace `cert-manager`.
#   kubectl apply -k localDeployment/ca-http-server/
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - configmap-index.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
